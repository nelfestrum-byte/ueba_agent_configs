# /etc/fluent-bit/conf.d/54-waf-metadata.conf
# Метаданные для WAF / ModSecurity logs (waf / freeipa pipeline).
# Управляется Ansible (роль fb_waf). Не редактировать вручную.

[FILTER]
    Name    record_modifier
    Match   waf_json
    Record  source_type modsecurity
