# /etc/fluent-bit/conf.d/53-suricata-metadata.conf
# Метаданные для Suricata EVE JSON (suricata / freeipa pipeline).
# Управляется Ansible (роль fb_suricata). Не редактировать вручную.

[FILTER]
    Name    record_modifier
    Match   suricata_json
    Record  source_type suricata
