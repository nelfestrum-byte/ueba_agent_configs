[SERVICE]
    # Flush
    # =====
    # set an interval of seconds before to flush records to a destination
    flush        1

    # Daemon
    # ======
    # instruct Fluent Bit to run in foreground or background mode.
    daemon       Off

    # Log_Level
    # =========
    # Set the verbosity level of the service, values can be:
    #
    # - error
    # - warning
    # - info
    # - debug
    # - trace
    #
    # by default 'info' is set, that means it includes 'error' and 'warning'.
    log_level    info

    Log_File    /var/log/fluent-bit-debug.log
    # Parsers File
    # ============
    # specify an optional 'Parsers' configuration file
    parsers_file parsers.conf

    # Plugins File
    # ============
    # specify an optional 'Plugins' configuration file to load external plugins.
    plugins_file plugins.conf

    # HTTP Server
    # ===========
    # Enable/Disable the built-in HTTP Server for metrics
    http_server  On
    http_listen  0.0.0.0
    http_port    2020

    # Storage
    # =======
    # Fluent Bit can use memory and filesystem buffering based mechanisms
    #
    # - https://docs.fluentbit.io/manual/administration/buffering-and-storage
    #
    # storage metrics
    # ---------------
    # publish storage pipeline metrics in '/api/v1/storage'. The metrics are
    # exported only if the 'http_server' option is enabled.
    #
    storage.metrics on

    # storage.path
    # ------------
    # absolute file system path to store filesystem data buffers (chunks).
    #
    # storage.path /tmp/storage
    storage.path /opt/fluent-bit/storage

    # storage.sync
    # ------------
    # configure the synchronization mode used to store the data into the
    # filesystem. It can take the values normal or full.
    #
    storage.sync full

    # storage.checksum
    # ----------------
    # enable the data integrity check when writing and reading data from the
    # filesystem. The storage layer uses the CRC32 algorithm.
    #
    # storage.checksum off

    # storage.backlog.mem_limit
    # -------------------------
    # if storage.path is set, Fluent Bit will look for data chunks that were
    # not delivered and are still in the storage layer, these are called
    # backlog data. This option configure a hint of maximum value of memory
    # to use when processing these records.
    #
#    storage.backlog.mem_limit 50M
    storage.max_chunks_up 10

[INPUT]
    name tail
    storage.type filesystem
    path /var/log/osquery/osqueryd.*.log
    read_from_Head true
    parser json
    tag osquery_json
    DB /opt/fluent-bit/osquery_db
    DB.sync full
    DB.locking true
    DB.journal_mode WAL
#    Buffer_Chunk_Size 32000B
#    Buffer_Max_Size 4000000B
# Read interval (sec) Default: 1
#    interval_sec 1

[INPUT]
    name             forward
    storage.type     filesystem


[INPUT]
    name             docker_events
    storage.type     filesystem
    tag              docker_events
    parser           json
    Unix_Path        /var/run/docker.sock
#[INPUT]
#    name             docker_events
#    storage.type     filesystem
#    tag              docker_events
#    parser           json

[INPUT]
    name tail
    storage.type filesystem
    path /opt/suricata/logs/eve.json
    read_from_Head true
    parser json
    tag suricata_json
    DB /opt/fluent-bit/suricata_db
    DB.sync full
    DB.locking true
    DB.journal_mode WAL

[INPUT]
    name tail
    storage.type filesystem
    path /opt/nginx/logs/modsecurity.log
    read_from_Head false
    parser json
    tag waf_json
    DB /opt/fluent-bit/waf_db
    DB.sync full
    DB.locking true
    DB.journal_mode WAL
#    Buffer_Chunk_Size 32000B
#    Buffer_Max_Size 4000000B

[INPUT]
    Name             tail
    storage.type     filesystem
    Tag              freeipa.kdc
    Path             /var/lib/ipa-data/var/log/krb5kdc.log
    Parser           ipa_kdc
    DB               /opt/fluent-bit/kdc.db
    DB.sync          full
    DB.locking       true
    DB.journal_mode  WAL

[INPUT]
    Name             tail
    storage.type     filesystem
    Tag              freeipa.httpd.access
    Path             /var/lib/ipa-data/var/log/httpd/access_log
    Parser           apache_access
    DB               /opt/fluent-bit/httpd_access.db
    DB.sync          full
    DB.locking       true
    DB.journal_mode  WAL

[INPUT]
    Name             tail
    storage.type     filesystem
    Tag              freeipa.httpd.error
    Path             /var/lib/ipa-data/var/log/httpd/error_log
    Parser           apache_error
    DB               /opt/fluent-bit/httpd_error.db
    DB.sync          full
    DB.locking       true
    DB.journal_mode  WAL

[INPUT]
    Name             tail
    storage.type     filesystem
    Tag              freeipa.dirsrv.access
    Path             /var/lib/ipa-data/var/log/dirsrv/slapd-*/access
    Parser           dirsrv_access
    DB               /opt/fluent-bit/dirsrv_access.db
    DB.sync          full
    DB.locking       true
    DB.journal_mode  WAL

[INPUT]
    Name             tail
    storage.type     filesystem
    Tag              freeipa.dirsrv.error
    Path             /var/lib/ipa-data/var/log/dirsrv/slapd-*/errors
    Parser           dirsrv_error
    DB               /opt/fluent-bit/dirsrv_error.db
    DB.sync          full
    DB.locking       true
    DB.journal_mode  WAL

[INPUT]
    Name             tail
    storage.type     filesystem
    Tag              freeipa.dirsrv.audit
    Path             /var/lib/ipa-data/var/log/dirsrv/slapd-*/audit
    Parser           dirsrv_audit
    DB               /opt/fluent-bit/dirsrv_audit.db
    DB.sync          full
    DB.locking       true
    DB.journal_mode  WAL

[INPUT]
    Name             tail
    storage.type     filesystem
    Tag              freeipa.dirsrv.auditfail
    Path             /var/lib/ipa-data/var/log/dirsrv/slapd-*/auditfail 
    Parser           dirsrv_audit
    DB               /opt/fluent-bit/dirsrv_auditfail.db
    DB.sync          full
    DB.locking       true
    DB.journal_mode  WAL

[INPUT]
    Name             tail
    storage.type     filesystem
    Tag              freeipa.dogtag
    Path             /var/lib/ipa-data/var/log/pki/pki-tomcat/ca/debug
    Parser           dogtag
    multiline.parser java_stacktrace
    DB               /opt/fluent-bit/dogtag.db
    DB.sync          full
    DB.locking       true
    DB.journal_mode  WAL

[INPUT]
    Name             tail
    storage.type     filesystem
    Tag              freeipa.dns
    Path             /var/lib/ipa-data/var/log/named/*.log,/var/lib/ipa-data/var/log/named/data/named.run
    Parser           bind_dns
    DB               /opt/fluent-bit/dns.db
    DB.sync          full
    DB.locking       true
    DB.journal_mode  WAL

[INPUT]
    Name             tail
    storage.type     filesystem
    Tag              freeipa.sssd
    Path             /var/lib/ipa-data/var/log/sssd/*.log
    Parser           sssd
    DB               /opt/fluent-bit/sssd.db
    DB.sync          full
    DB.locking       true
    DB.journal_mode  WAL

[FILTER]
    Name        modify
    Match       freeipa.httpd.access
    Rename      source_ip source.ip
    Rename      user_name user.name
    Rename      http_request_method http.request.method
    Rename      url_path url.path
    Rename      http_response_status_code http.response.status_code
    Rename      http_response_body_bytes http.response.body.bytes

[FILTER]
    Name        modify
    Match       freeipa.dirsrv.access
    Rename      ldap_connection_id ldap.connection.id
    Rename      ldap_operation_id ldap.operation.id

[FILTER]
    Name        modify
    Match       freeipa.dns
    Rename      dns_category dns.category

[FILTER]
    Name        modify
    Match       freeipa.dirsrv.audit*
    Rename      ldap_dn ldap.dn

[FILTER]
    Name             modify
    Match            freeipa.kdc
    Add              service.name kerberos

[FILTER]
    Name             modify
    Match            freeipa.httpd.*
    Add              service.name httpd

[FILTER]
    Name             modify
    Match            freeipa.dirsrv.*
    Add              service.name ldap

[FILTER]
    Name             modify
    Match            freeipa.dns
    Add              service.name dns

[FILTER]
    Name             modify
    Match            freeipa.dogtag
    Add              service.name dogtag

[FILTER]
    Name             modify
    Match            freeipa.sssd
    Add              service.name sssd

[FILTER]
    Name             lua
    Match            freeipa.*
    script           scripts/freeipa.lua
    call             normalize

[FILTER]
    Name    grep
    Match   docker.*
    Regex   log .+

[FILTER]
    name                  multiline
#    multiline.parser      docker
    match                 docker.*
    multiline.key_content log
    mode                  partial_message

[FILTER]
    Name         parser
    Parser       docker
    Match        docker.*
    Key_Name     log
    Reserve_Data true
    Preserve_Key false

[FILTER]
    Name    record_modifier
    Match   *
    Record  fqdn ${HOSTNAME}

[FILTER]
    Name    record_modifier
    Match   freeipa.*
    Record  source_type freeipa

[FILTER]
    Name    record_modifier
    Match   docker.*
    Record  source_type docker_container

[FILTER]
    Name    record_modifier
    Match   docker_events
    Record  source_type docker_events

[FILTER]
    Name    record_modifier
    Match   suricata_json
    Record  source_type suricata

[FILTER]
    Name    record_modifier
    Match   waf_json
    Record  source_type modsecurity

[FILTER]
    Name    record_modifier
    Match   osquery_json
    Record  source_type osquery

[OUTPUT]
    Name           tcp
    Match          *
    Host           10.202.77.84
    Port           5044
    Format         json_lines
    Retry_Limit    5

#[OUTPUT]
#    Name   stdout
#    Match  *
